require 'spec_helper'

module DaButtonFactory
  
  describe "The api request generated for a valid button" do
    
    it "should have a text param if text has been specified" do
      pending
    end

    it "should contain a font param if a font has been specified" do
      pending
    end
    
    it "should add -Bold to the font param if bold has been selected and a font has been set" do
      pending
    end
    
    it "should add -Italic to the font param if italic has been selected and a font has been set" do
      pending
    end
    
    it "should add -Bold-Italic to the font param if bold and italic have been selected and a font has been set" do
      pending
    end
    
    it "should have a text-color param if a text colour has been specified" do
      pending
    end

    it "should have a text-size param if a text size has been specified" do
      pending
    end

    it "should have a text-shadow-spacing param if a text shadow spacing has been specified" do
      pending
    end

    it "should have a text-shadow-color param if a text shadow colour has been specified" do
      pending
    end

    it "should have a corners param if a corner radius has been specified" do
      pending
    end

    it "should have a bg-type param if a background colouration type has been specified" do
      pending
    end

    it "should have a bg-color param if a background colour has been specified" do
      pending
    end

    it "should have a end-bg-color param if a second background colour has been specified" do
      pending
    end

    it "should have a border-size param if a border size has been specified" do
      pending
    end

    it "should have a border-color param if a border colour has been specified" do
      pending
    end

    it "should have a shadow-size param if a shadow size has been specified" do
      pending
    end

    it "should have a shadow-color param if a shadow colour has been specified" do
      pending
    end

    it "should have a shadow-orient param if a shadow orientation has been specified" do
      pending
    end

    it "should have a bubble-effect param if bubble effect has been selected" do
      pending
    end

    it "should have a hpadding param if a horizontal padding has been specified" do
      pending
    end

    it "should have a vpadding param if a vertical padding has been specified" do
      pending
    end

    it "should have a width param if a width has been specified" do
      pending
    end

    it "should have a height param if a height has been specified" do
      pending
    end

    it "should have a type param if a file type has been specified" do
      pending
    end
    
  end

end
